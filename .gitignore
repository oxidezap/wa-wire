# Rust
/target
**/target

# Node
node_modules/
dist/

# Coverage
coverage/
*.profraw
*.profdata
lcov.info

# Editors and OS
.DS_Store
*.swp

# Python
__pycache__/

# The Go adapter's module cache artefacts. `go.sum` is committed; the rest is
# the toolchain's.
adapters/hypermeow/vendor/

# TypeScript adapter builds and their local dependency links.
adapters/*/dist/

# Replays are regenerated from the corpus by each adapter's replay command, and
# the conformance test reads them. Committed would be a fourth description of
# what the engines produce, kept in step by hand.
adapters/*/replay/
tools/translation-bench/target/
