[package]
name = "wa-wire-conformance"
version = "0.1.0"
description = "Replays recorded stanzas through every engine and requires them to agree"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
wa-wire-adapter = { path = "../wa-wire-adapter" }
wa-wire-codec = { path = "../wa-wire-codec" }
wa-wire-contract = { path = "../wa-wire-contract" }
wa-wire-l1 = { path = "../wa-wire-l1" }

[dev-dependencies]
wa-wire-l1 = { path = "../wa-wire-l1", features = ["testing"] }

[lints]
workspace = true
