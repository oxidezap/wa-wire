[package]
name = "wa-wire-proto"
version = "0.1.0"
description = "Zero-copy reader for the protobuf wire format, over payloads the boundary carries"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

keywords = ["whatsapp", "protobuf", "protocol", "no-std"]
categories = ["network-programming", "encoding", "no-std"]
readme = "README.md"
documentation = "https://docs.rs/wa-wire-proto"

[dependencies]

[lints]
workspace = true
